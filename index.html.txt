.
<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LUXURY BOUTIQUE</title>
    <style>
        /* STILE LUXURY - Nero, Bianco e Accenti Oro */
        :root {
            --bg-color: #0d0d0d;
            --card-bg: #171717;
            --accent-gold: #d4af37;
            --text-color: #ffffff;
            --text-secondary: #a3a3a3;
            --font-family: 'Playfair Display', Georgia, serif;
        }

        body {
            margin: 0;
            padding: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            -webkit-font-smoothing: antialiased;
        }

        /* HEADER E NAVIGAZIONE */
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            border-bottom: 1px solid #262626;
            position: sticky;
            top: 0;
            background: rgba(13, 13, 13, 0.95);
            backdrop-filter: blur(10px);
            z-index: 100;
        }

        .logo {
            font-family: var(--font-family);
            font-size: 22px;
            letter-spacing: 3px;
            color: var(--accent-gold);
            text-transform: uppercase;
            font-weight: bold;
        }

        .cart-icon {
            font-size: 18px;
            cursor: pointer;
            position: relative;
        }

        .cart-count {
            background-color: var(--accent-gold);
            color: #000;
            font-size: 11px;
            font-weight: bold;
            padding: 2px 6px;
            border-radius: 50%;
            margin-left: 5px;
        }

        /* HERO BANNER */
        .hero {
            text-align: center;
            padding: 60px 20px;
            background: linear-gradient(180deg, #171717 0%, #0d0d0d 100%);
            border-bottom: 1px solid #262626;
        }

        .hero h1 {
            font-family: var(--font-family);
            font-size: 32px;
            letter-spacing: 2px;
            margin-bottom: 10px;
            color: var(--text-color);
        }

        .hero p {
            color: var(--text-secondary);
            font-size: 14px;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        /* GRIGLIA PRODOTTI */
        .container {
            padding: 20px;
            max-width: 600px;
            margin: 0 auto;
        }

        .section-title {
            font-family: var(--font-family);
            font-size: 20px;
            letter-spacing: 2px;
            text-align: center;
            margin: 30px 0 20px 0;
            color: var(--accent-gold);
            text-transform: uppercase;
        }

        .product-card {
            background-color: var(--card-bg);
            border: 1px solid #262626;
            border-radius: 8px;
            overflow: hidden;
            margin-bottom: 30px;
        }

        .product-image {
            width: 100%;
            height: 350px;
            object-fit: cover;
            display: block;
        }

        .product-info {
            padding: 20px;
        }

        .product-title {
            font-family: var(--font-family);
            font-size: 18px;
            margin: 0 0 8px 0;
            letter-spacing: 1px;
        }

        .product-price {
            color: var(--accent-gold);
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 15px;
        }

        /* SELEZIONE TAGLIE */
        .size-label {
            font-size: 12px;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
            display: block;
        }

        .size-options {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        .size-btn {
            background: transparent;
            border: 1px solid #404040;
            color: var(--text-color);
            padding: 8px 14px;
            border-radius: 4px;
            font-size: 12px;
            cursor: pointer;
        }

        .size-btn.active {
            border-color: var(--accent-gold);
            color: var(--accent-gold);
            background: rgba(212, 175, 55, 0.1);
        }

        /* PULSANTE ACQUISTA */
        .btn-add {
            width: 100%;
            background-color: var(--accent-gold);
            color: #000;
            border: none;
            padding: 14px;
            font-size: 13px;
            font-weight: bold;
            letter-spacing: 2px;
            text-transform: uppercase;
            border-radius: 4px;
            cursor: pointer;
        }

        /* FOOTER */
        footer {
            text-align: center;
            padding: 30px 20px;
            border-top: 1px solid #262626;
            color: var(--text-secondary);
            font-size: 12px;
        }
    </style>
</head>
<body>

    <header>
        <div class="logo">Maison Luxury</div>
        <div class="cart-icon">
            🛒 Carrello <span class="cart-count" id="cart-count">0</span>
        </div>
    </header>

    <section class="hero">
        <h1>COLLEZIONE ESCLUSIVA</h1>
        <p>Edizione Limitata & Design Unico</p>
    </section>

    <div class="container">
        <div class="section-title">Prodotti In Evidenza</div>

        <!-- SCHEDA PRODOTTO 1 -->
        <div class="product-card">
            <img src="https://images.unsplash.com/photo-1539109136881-3be0616acf4b?auto=format&fit=crop&w=800&q=80" alt="Giacca Luxury" class="product-image">
            <div class="product-info">
                <h2 class="product-title">Giacca Haute Couture</h2>
                <div class="product-price">€ 1.250,00</div>
                
                <span class="size-label">Seleziona Taglia:</span>
                <div class="size-options">
                    <button class="size-btn" onclick="selectSize(this)">S</button>
                    <button class="size-btn active" onclick="selectSize(this)">M</button>
                    <button class="size-btn" onclick="selectSize(this)">L</button>
                    <button class="size-btn" onclick="selectSize(this)">XL</button>
                </div>

                <button class="btn-add" onclick="addToCart()">Aggiungi al Carrello</button>
            </div>
        </div>
    </div>

    <footer>
        <p>© 2026 Maison Luxury. Tutti i diritti riservati.</p>
        <p>Contatti: concierge@maisonluxury.com</p>
    </footer>

    <script>
        let cartItems = 0;

        function selectSize(element) {
            let buttons = element.parentElement.querySelectorAll('.size-btn');
            buttons.forEach(btn => btn.classList.remove('active'));
            element.classList.add('active');
        }

        function addToCart() {
            cartItems++;
            document.getElementById('cart-count').innerText = cartItems;
            alert('Articolo aggiunto al carrello con successo!');
        }
    </script>
</body>
</html>
